package leetcode

fun Task797() {
    allPathsSourceTarget(
        arrayOf(
            intArrayOf(1, 2),
            intArrayOf(3),
            intArrayOf(3),
            intArrayOf(),
        )
    )
}

private fun allPathsSourceTarget(graph: Array<IntArray>): List<List<Int>> {
return emptyList()
}